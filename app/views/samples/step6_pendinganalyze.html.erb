<div class="container">

  <br>
  <h1>Upload Test Results</h1>
  <br>

    <table id="datatable" class="table table-striped table-bordered">
      <thead class="thead-dark">
      <tr>
        <th>Plate</th>
        <th>Results</th>
      </tr>
      </thead>
      <tbody>
      <% @plates.each do |plate| %>
        <tr>
          <td>
            <%= link_to plate do  %>
              <%= tag.button class: 'btn btn-sm btn-outline-primary' do  %>
                Show
              <% end  %>
            <% end  %>
          </td>
          <td>
            <%= link_to new_plate_test_path(plate) do  %>
              <%= tag.button class: 'btn btn-small btn-primary' do  %>
                Upload
              <% end  %>
            <% end  %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>

</div>