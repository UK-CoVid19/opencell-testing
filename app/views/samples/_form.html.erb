<br/>
<%= form_with(model: sample, local: true, html: {class: 'form-inline'}) do |form| %>
  <% if sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sample.errors.count, "error") %> prohibited this sample from being saved:</h2>

      <ul>
        <% sample.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group mx-sm-3 mb-2">
    <%= form.label :client_id, "Client", class: 'sr-only' %>
    <%= form.collection_select :client_id, Client.all, :id, :name, {}, {class: 'form-control'} %>
  </div>
  <%= form.submit class: 'btn btn-primary mb-2' %>
<% end %>
  